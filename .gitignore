(index.html is a source file, so no additional ignores are needed beyond what might already be in place for general project types. Since only a single HTML file was modified and no build artifacts, dependencies, or temp files were mentioned, the optimal action is to output nothing.)

# Nothing to add - only source/config files detected